# Railway Environment Variables Template
# Copy these to your Railway service variables

# ======================
# API Service Variables
# ======================

# Database (Railway provides this)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Frontend URL (update with your Railway web service URL)
FRONTEND_URL=https://your-web-service.railway.app

# Better Auth (generate a random secret)
BETTER_AUTH_SECRET=<generate-random-secret-min-32-chars>
BETTER_AUTH_URL=https://your-api-service.railway.app

# Google AI (if using AI features)
GOOGLE_AI_API_KEY=<your-google-ai-api-key>

# Server Configuration
PORT=3000
NODE_ENV=production

# ======================
# Web Service Variables
# ======================

# API URL (update with your Railway API service URL)
NEXT_PUBLIC_API_URL=https://your-api-service.railway.app

# Better Auth (must match API service)
BETTER_AUTH_URL=https://your-api-service.railway.app
BETTER_AUTH_SECRET=<same-as-api-service>

# Server Configuration
PORT=3000
NODE_ENV=production

# ======================
# Migration Job Variables
# ======================

# Database (Railway provides this)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# ======================
# Notes
# ======================

# 1. Replace all <placeholders> with actual values
# 2. Update all Railway URLs with your actual service URLs
# 3. Use the same BETTER_AUTH_SECRET for both API and Web
# 4. Generate BETTER_AUTH_SECRET: openssl rand -base64 32
# 5. Railway references: ${{Postgres.DATABASE_URL}} links to your Postgres service
